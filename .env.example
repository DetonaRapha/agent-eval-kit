# Copy to .env and fill in to enable the optional Claude (Anthropic) judge.
# The kit runs fully without this — the default MockJudge needs no key.
ANTHROPIC_API_KEY=

# Optional: override the judge model (sensible default is set in code).
# AGENT_EVAL_JUDGE_MODEL=claude-sonnet-5
